# @PACKAGE_INIT@ expands to helper macros (set_and_check, check_required_components)
# and makes ${PACKAGE_PREFIX_DIR} point to the install root at find_package() time.
@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/MyLibTargets.cmake")
check_required_components(MyLib)
